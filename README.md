# time-wizard

> A time tracking terminal application

## Install

Using yarn:

```sh
$ yarn global add time-wizard
```

Using npm:

```sh
$ npm install -g time-wizard
```

## Usage

```sh
$ time-wizard --help
```
